# Fixture Frontend App

Fixture Frontend App is a React dashboard that renders repository insight cards and talks to a backend API.

## Setup

`npm install`

## Usage

`npm run dev`

## Architecture

Vite boots the React application from `src/main.jsx`, and the UI calls `/api/projects` through a small API client.
